
export default async function (argv: any): Promise<void> {

    //
    // invoke the build command (dev mode)
    //    
    //  generate the docker command line parameters
    //  up the container (either in detatched or non-detatched mode)
    //
}